import { it, describe, expect, vi } from 'vitest';

/* Tests sur la fonction getEvents */
describe("fonction getEvents", () => {
    it("Should return filtered results",()=>{
       expect(1+1).toBe(2);
})
});
